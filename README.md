🛠️ ASPC Admin Panel
ASPC Admin Panel — это настольное приложение на Python с графическим интерфейсом (Tkinter) для управления учебными данными колледжа. Оно интегрируется с базой данных MySQL и поддерживает автоматические миграции, безопасное хранение конфигурации через .env, а также работу с учебными планами, студентами, педагогами и отчётами.

📦 ВОЗМОЖНОСТИ
Управление уроками и КТП (разделы, критерии, часы, типы занятий)

Работа с педагогами (ФИО, должности)

Учёт учеников (ФИО, дата рождения, группа)

Загрузка и хранение учебных планов с файлами

Социальный паспорт группы (многодетные, малообеспеченные, сироты и др.)

Учёт оценок по семестрам (s1, s2) и финальный расчёт

Протоколы экзаменов с привязкой к педагогам и группе

Автоматическое создание и миграция таблиц при первом запуске

Безопасное подключение к БД через .env файл

🧰 ТЕХНОЛОГИИ
Компонент	Используется

Язык Python 3.10+

GUI	Tkinter

База данных	MySQL

IDE	PyCharm

ORM	mysql.connector

Миграции Встроенные SQL-скрипты

Конфигурация python-dotenv

Работа с БД	MySQL Workbench (рекомендуется для визуального контроля)

ИНСТРУКЦИЯ

УСТАНОВКА ЗАВИСИМОСТЕЙ

Убедитесь, что у вас установлен Python 3.10+
Установите необходимые пакеты:

pip install mysql-connector-python python-dotenv

РАЗВЁРТЫВАНИЕ БАЗЫ ДАННЫХ

Создайте БД в MySQL: CREATE DATABASE education_manager CHARACTER SET utf8mb4;

Импортируйте дамп: mysql -u root -p education_manager < edu.sql

СОЗДАЙТЕ ФАЙЛ .env

Так как пароль к базе данных НЕ хранится в репозитории, нужно создать свой файл .env рядом с config.py.

DB_HOST=127.0.0.1

DB_USER=root

DB_PASSWORD=ваш_пароль

DB_NAME=education_manager

СТРУКТУРА КОНФИГУРАЦИИ

config.py автоматически загружает .env

from dotenv import load_dotenv

import os

load_dotenv()

DB_CONFIG = {
    'host': os.getenv("DB_HOST", "127.0.0.1"),
    'user': os.getenv("DB_USER", "root"),
    'password': os.getenv("DB_PASSWORD", ""),
    'database': os.getenv("DB_NAME", "education_manager"),
    'charset': 'utf8mb4'
}
Если .env нет — подключение НЕ сработает.

Запуск программы
